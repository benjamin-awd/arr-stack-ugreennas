NAS_IP_ADDRESS=1.2.3.4

# Domain Configuration
# Use a domain managed by Cloudflare (for automatic SSL certificates via DNS challenge)
# Example: yourdomain.com or a cheap domain like yourname-nas.com
DOMAIN=yourdomain.com

# Cloudflare Configuration (for SSL/TLS certificates)
# Generate API token at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit, Zone:Zone:Read for the domain
CF_DNS_API_TOKEN=your_cloudflare_api_token_here

# Cloudflare Tunnel Token (for external access without port forwarding)
# Get from: https://one.dash.cloudflare.com/ → Networks → Tunnels → Create/Configure
# Only required if using Cloudflare Tunnel (recommended for CGNAT/no port forwarding)
TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# Surfshark VPN Credentials (WireGuard)
# Get credentials from: https://my.surfshark.com/ → VPN → Manual Setup → Router → WireGuard
# 1. Select "I don't have a key pair" to generate new keys
# 2. Click "Download" to get the full WireGuard configuration file
# 3. Open the downloaded .conf file and extract:
#    - PrivateKey (from [Interface] section)
#    - Address (from [Interface] section, e.g., 10.14.0.2/16)
VPN_SERVICE_PROVIDER=your_provider
WIREGUARD_PRIVATE_KEY=your_wireguard_private_key_here
WIREGUARD_ADDRESS=10.14.0.2/16

# VPN Configuration (optional)
# Comma-separated list of countries (default: United Kingdom)
VPN_COUNTRIES=United Kingdom

# Pi-hole Admin Password
# Choose a strong password for the Pi-hole web interface
PIHOLE_UI_PASS=your_secure_pihole_password

# WireGuard Password Hash
# Generate with: docker run --rm ghcr.io/wg-easy/wg-easy wgpw 'YOUR_PASSWORD'
# IMPORTANT: Must be quoted (contains $ which Docker interprets as variables)
WG_PASSWORD_HASH='$2a$12$your_bcrypt_hash_here'

# Traefik Dashboard Authentication
# Generate with: htpasswd -nbB admin 'YOUR_PASSWORD'
# IMPORTANT: Must be quoted (contains $ which Docker interprets as variables)
TRAEFIK_DASHBOARD_AUTH='admin:$2y$05$your_bcrypt_hash_here'

# Immich Configuration
# Immich version to use (default: release)
IMMICH_VERSION=release

# Database configuration
DB_USERNAME=immich
DB_PASSWORD=password123
DB_DATABASE_NAME=immich

# Storage locations (must be absolute paths)
# Optimized for HDD (volume1) + SSD (volume2) setup:
# - Large files (originals, encoded videos) → volume1 (HDD)
# - Small frequently-accessed files (thumbs, profiles) → volume2 (SSD)
# - Database and backups → volume2 (SSD)

# UPLOAD_LOCATION: Original uploaded photos/videos (large files, HDD)
UPLOAD_LOCATION=/volume1/Media/immich

# THUMB_LOCATION: Thumbnails for timeline and preview (small files, random access, SSD)
THUMB_LOCATION=/volume2/docker/arr-stack/immich/thumbs

# ENCODED_VIDEO_LOCATION: Transcoded videos (large files, HDD)
ENCODED_VIDEO_LOCATION=/volume1/Media/immich/encoded-video

# PROFILE_LOCATION: User profile pictures (tiny <1MB total, SSD)
PROFILE_LOCATION=/volume2/docker/arr-stack/immich/profile

# BACKUP_LOCATION: Database backups (medium size, SSD)
BACKUP_LOCATION=/volume2/docker/arr-stack/immich/backups

# DB_DATA_LOCATION: PostgreSQL database (metadata, search indices, SSD)
DB_DATA_LOCATION=/volume2/docker/arr-stack/immich/postgres

# Optional: PUID/PGID (default: 1000/1000)
# Find your user ID with: id -u
# Find your group ID with: id -g
# PUID=1000
# PGID=1000

# Optional: Timezone (default: Europe/London)
# TZ=Europe/London

# Optional: Plex (if using docker-compose.plex-arr-stack.yml instead of Jellyfin)
# Get claim token from: https://plex.tv/claim (expires in 4 minutes!)
# Only needed on first run to link server to your Plex account
# PLEX_CLAIM=claim-xxxxxxxxxxxxxxxxxxxx
